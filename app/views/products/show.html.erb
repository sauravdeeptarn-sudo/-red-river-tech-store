<div class="row">
  <div class="col-md-8">
    <nav aria-label="breadcrumb">
      <ol class="breadcrumb">
        <li class="breadcrumb-item"><%= link_to "Home", root_path %></li>
        <li class="breadcrumb-item"><%= @product.category.name %></li>
        <li class="breadcrumb-item active"><%= @product.name %></li>
      </ol>
    </nav>

    <h1><%= @product.name %></h1>
    <span class="badge bg-primary mb-3"><%= @product.category.name %></span>
    <p class="lead"><%= @product.description %></p>

    <div class="card mt-4">
      <div class="card-body">
        <h3 class="text-success">$<%= number_with_precision(@product.price, precision: 2) %></h3>
        <p class="text-muted">In Stock: <%= @product.stock_quantity %> units</p>
        <%= button_to "🛒 Add to Cart", add_to_cart_path(@product), method: :post, class: "btn btn-success btn-lg" %>
        <%= link_to "Back to Products", root_path, class: "btn btn-outline-secondary btn-lg ms-2" %>
      </div>
    </div>
  </div>
</div>